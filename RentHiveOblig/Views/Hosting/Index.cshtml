@using Microsoft.AspNetCore.Identity;
@model RentHiveOblig.ViewModels.HostingDashBViewModel;
@inject UserManager<ApplicationUser> UserManager
@{
    ViewData["Title"] = "Control";
    var userId = UserManager.GetUserId(User);
    var user = UserManager.FindByIdAsync(userId).Result;
}

<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
<link rel="stylesheet" href="~/RentHiveOblig.styles.css" asp-append-version="true" />
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@300&display=swap" rel="stylesheet">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<h1>Welcome back @user.Firstname</h1>

<br />
<br />

<!--Should be able to see the users published listings -->
<!--Should be able to go in a published listing and configure-->
<!--If no listings is set on the user, then give different text something like "You do currently not have any listings-->


<div id="Availlistings" class="shadow p-3 mb-5 bg-white rounded">

    <h2>Available Listings</h2>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Title</th>
                <th>Description</th>

                <!--For the buttons-->
                <th>Created at</th>
                <th></th>
                <th></th>
            </tr>
        </thead>


        <tbody>
            @foreach (var item in Model.Eiendoms)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Tittel)</td>
                    <td>@Html.DisplayFor(modelItem => item.Beskrivelse)</td>
                    <td>@Html.DisplayFor(modelItem => item.CreatedDateTime)</td>


                    <!--Buttons to edit/delete the listing-->
                    <td><a asp-action="Edit" asp-controller="Eiendoms" asp-route-id="@item.EiendomID">Edit</a></td>
                    <td><a asp-action="Delete" asp-controller="Eiendoms" asp-route-id="@item.EiendomID">Delete</a></td>

                </tr>
            }
        </tbody>
    </table>

    <a asp-controller="Eiendoms" asp-action="Create" class="btn btn-primary">Create a new listing</a>


</div>

<!--Should be able to control reservations/bookings-->

<br />
<br />





<!--  Show pending reservations  -->
<div id="pendingRes" class="shadow p-3 mb-5 bg-white rounded">
    <h2>Pending Reservations:</h2>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Booking ID</th>
                <th>Property ID</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Total Price</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var booking in Model.PendingBookings)
            {
                <tr>
                    <td>@booking.BookingId</td>
                    <td>@booking.EiendomId</td>
                    <td>@booking.StartDate</td>
                    <td>@booking.EndDate</td>
                    <td>@booking.TotalPrice</td>
                    <td>
                        <button class="btn btn-primary">Accept</button>
                        <button class="btn btn-danger">Decline</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>



<!--Show approved reservations-->

<div id="approvedRes" class="shadow p-3 mb-5 bg-white rounded">
    <h2>Approved Reservations:</h2>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Booking ID</th>
                <th>Property ID</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Total Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var booking in Model.ApprovedBookings)
            {
                <tr>
                    <td>@booking.BookingId</td>
                    <td>@booking.EiendomId</td>
                    <td>@booking.StartDate</td>
                    <td>@booking.EndDate</td>
                    <td>@booking.TotalPrice</td>
                </tr>
            }
        </tbody>
    </table>
</div>


