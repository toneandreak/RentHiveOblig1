@model IEnumerable<RentHiveOblig.Models.Bruker>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrukerNavn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrukerEpost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrukerNavn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrukerEpost)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BrukerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BrukerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BrukerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
