@model RentHiveOblig.Models.WishlistEiendom

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>WishlistEiendom</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WishListEiendomId" />
            <div class="form-group">
                <label asp-for="WishlistId" class="control-label"></label>
                <select asp-for="WishlistId" class="form-control" asp-items="ViewBag.WishlistId"></select>
                <span asp-validation-for="WishlistId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EiendomId" class="control-label"></label>
                <select asp-for="EiendomId" class="form-control" asp-items="ViewBag.EiendomId"></select>
                <span asp-validation-for="EiendomId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
